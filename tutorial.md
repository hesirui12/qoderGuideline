---
title: Qoder 零基础实战教程（2026 版）
subtitle: 从安装到 DevOps —— 50 章全面掌握 Qoder 国际版与国内版
version: "2026"
date: 2026-02
author: 计算机大破冰
---

# Qoder 零基础实战教程（2026 版）

> 本教程面向完全没有用过 Qoder 的开发者，从注册安装讲起，覆盖写代码、Git 版本管理、Skills、代码审查、DevOps、技术栈限定、提示词工程等核心主题，共 50 章，每章约 50 行，可直接当手册查阅。
>
> **阅读建议**：初学者按顺序通读；有经验者可按目录跳读。文中命令、价格、模型名以官网最新文档为准，本教程整理自 2026 年初公开资料。

---

# 第一部分 认识 Qoder 与安装

## 第1章 Qoder 是什么：新一代 Agentic 编码平台

### 1.1 一句话定位

Qoder 是阿里巴巴旗下团队推出的 **Agentic（智能体式）编码平台**，于 2025 年 8 月前后正式发布，早期国内版本与「通义灵码 / Lingma」产品线一脉相承。它不止是代码补全工具，而是能**理解整个代码库、自主规划并执行多步任务**的 AI 编程搭档。

### 1.2 2026 年的 Qoder 生态

到了 2026 年，Qoder 已经扩展为完整产品矩阵，核心能力可以概括为「一个引擎、三层知识、一套协议」：

| 能力 | 说明 |
| --- | --- |
| Quest 任务引擎 | 独立视窗委派长程、多步任务，Agent 持续推进并交付成果 |
| Experts 专家团 | Lead Agent 动态调度多名专家子智能体并行工作 |
| 三层知识 | 项目规则（AGENTS.md/rules）+ 长期记忆 + 结构化知识库（Repo Wiki 等） |
| MCP 协议 | 通过 Model Context Protocol 接入外部工具与服务 |
| Skills 机制 | 把专业知识打包成可复用的 SKILL.md 技能 |

### 1.3 与 Cursor、Claude Code 的区别

- **Qoder**：阿里系，中英文双版本，IDE/CLI/插件全家桶，Quest 任务引擎突出；
- **Cursor**：编辑器体验成熟，插件生态丰富，偏「编辑器 + AI」；
- **Claude Code**：终端 Agent 先驱，强调纯 CLI 工作流；
- 社区评测普遍认为 Qoder 的 Agent 稳定性与记忆能力是其长板，适合中文用户与团队协作场景。

### 1.4 常见术语速查

- **Ask**：问答模式，只回答不改代码；
- **Agent**：智能体模式，可自主读文件、改代码、跑命令；
- **NEXT**：光标处的下一条编辑建议（类 Tab 补全）；
- **Quest**：独立视窗的长程任务委派；
- **Skills**：可复用技能包（SKILL.md）；
- **MCP**：外部工具接入协议；
- **Hooks**：在关键节点介入 Agent 流程的钩子；
- **Rules / AGENTS.md**：项目级指令与约定。

### 1.5 本章小结

- [ ] 理解 Qoder = Agentic 编码平台，而非普通补全工具
- [ ] 记住六大核心概念：Ask / Agent / NEXT / Quest / Skills / MCP
- [ ] 能说出它与 Cursor、Claude Code 的定位差异

## 第2章 国际版与国内版：怎么选

### 2.1 两个版本的存在

Qoder 分为两大版本线：

- **国际版**：官网 `qoder.com`，文档 `docs.qoder.com`，面向全球用户；
- **国内版（Qoder CN）**：官网 `qoder.cn`，文档 `docs.qoder.cn`，产品页挂在阿里云（通义灵码 Lingma 产品线）下，面向中国大陆用户。

### 2.2 主要差异对比

| 维度 | 国际版 | 国内版（Qoder CN） |
| --- | --- | --- |
| 官网 / 文档 | qoder.com | qoder.cn / 阿里云 |
| 登录方式 | Google、GitHub、邮箱注册 | 阿里云账号、手机号、钉钉 |
| 可用模型 | 模型选择更丰富（含海外模型） | 以 Qwen、GLM、Kimi、DeepSeek 等国内模型为主 |
| 付费方式 | 国际信用卡 / 网页支付 | 支付宝、微信等国内支付 |
| 功能节奏 | 社区反馈迭代较快 | 部分功能上架稍晚，但紧跟 |
| 数据合规 | 遵循国际数据政策 | 数据本地化，更适合国内合规要求 |

> ⚠️ 论坛反馈（非官方）：国内版按不同模型有不同的 credits 消耗倍率，且 IDE/CLI 与 Work 等订阅可能拆分售卖；国际版则打包订阅。具体以官网价格页为准。

### 2.3 价格参考（2026 年初，可能调整）

- 国际版 Pro：约 $30/月（含 credits），曾有促销价；credits 用完后回落到基础模型；
- 国内版个人专业版：约 ¥59/月 档位（含 QoderWork、QoderWake、JetBrains 插件、CLI）；
- 团队版：约 ¥99/人/月；企业版：约 ¥149/人/月（最少 10 人）；
- 个人版普遍提供**免费试用**，试用期限可能调整。

### 2.4 选择建议

- 个人学习、追求模型多样 → 国际版；
- 团队协作、数据合规、国内支付 → 国内版；
- 两边账号相互独立，不能混用 credits。

### 2.5 本章小结

- [ ] 明确国际版与国内版的官网与文档地址
- [ ] 对比登录方式、模型、价格后做出选择
- [ ] 记住：价格与功能以官网为准，试用期可能变动

## 第3章 注册与登录（国际版）

### 3.1 第一步：下载并启动 Qoder IDE

1. 访问 `qoder.com`，点击 Download 下载对应系统安装包；
2. 安装后启动，你会看到一个接近 VSCode 风格的编辑器。

### 3.2 打开登录面板

- 点击右上角**用户头像图标**，或按快捷键 `Ctrl + Shift + ,`（macOS 为 `Cmd + Shift + ,`）；
- 选择 **登录（Sign in）**，浏览器会自动打开登录页。

### 3.3 完成注册

在打开的网页中，任选一种方式：

1. 点击底部 **注册（Sign up）**，用邮箱创建账号；
2. 或直接用 **Google / GitHub 账号**一键登录。

> 💡 用 GitHub 登录可以顺便在后续演示「克隆 GitHub 仓库」时减少授权步骤。

### 3.4 回到 IDE 确认登录

- 浏览器提示授权成功后，回到 Qoder IDE；
- 右上角显示你的头像/账号，即登录成功；
- 个人版一般自动开通**免费试用**，可在设置页查看剩余试用额度。

### 3.5 登录失败排查

- 网络问题：国际版部分网络环境下需要代理，可换网络重试；
- 弹窗被拦截：允许浏览器弹窗；
- 反复要求授权：清理浏览器缓存后重试。

### 3.6 本章小结

- [ ] 完成国际版账号注册
- [ ] 在 IDE 内成功登录并看到头像
- [ ] 知道登录入口的快捷键是 Ctrl/Cmd + Shift + ,

## 第4章 注册与激活（国内版 Qoder CN）

### 4.1 进入国内版入口

1. 打开 `qoder.cn`，或访问阿里云官网搜索「通义灵码 / Qoder」；
2. 国内版通常要求使用**阿里云账号**登录，也可用手机号或钉钉账号。

### 4.2 开通流程

1. 注册/登录阿里云账号（未注册则先完成实名认证）；
2. 在产品页点击**开通**，选择个人版、团队版或企业版；
3. 个人版一般可免费试用；团队/企业版按人按月计费，需要填写组织信息。

### 4.3 下载客户端

- 国内版提供：Qoder IDE（国内版）、QoderWork（桌面办公助手）、QoderWake、JetBrains 插件、CLI；
- 在 `qoder.cn` 下载页选择对应系统的安装包；
- 安装后首次启动，用刚才的阿里云/手机号账号登录。

### 4.4 激活与绑定

- 团队版：管理员在控制台添加成员，成员登录后自动生效；
- 企业版：可能需要管理员分配 seat（席位），个人无法自行激活；
- 试用到期：可在控制台升级订阅，或接受回落基础模型。

### 4.5 与通义灵码插件的关系

- 国内版的 JetBrains/IDE 能力与此前的通义灵码（Lingma）插件同源演进，老用户可平滑迁移到新客户端。

### 4.6 本章小结

- [ ] 完成阿里云账号注册与实名认证
- [ ] 开通并登录 Qoder CN
- [ ] 知道个人版/团队版/企业版的激活方式差异

## 第5章 安装 Qoder IDE 与打开第一个项目

### 5.1 系统要求

- **Windows**：Windows 10/11 64 位；
- **macOS**：Apple Silicon 与 Intel 均可，建议 macOS 12+；
- **Linux**：常见发行版的 .deb/.rpm/AppImage 安装包。

### 5.2 安装步骤

1. 官网下载安装包；
2. Windows：双击运行安装向导；macOS：拖入 Applications；Linux：按发行版安装；
3. 首次启动按引导完成登录（见第3/4章）；
4. 首次启动会有简短的**功能引导**，建议看完，它正好演示 NEXT 与行间会话。

### 5.3 打开本地项目

- 启动页点击 **打开（Open）**，快捷键 `Ctrl + O`（macOS `Cmd + O`）；
- 选择项目文件夹即可；Qoder 会自动索引项目，稍等片刻即可提问。

### 5.4 克隆 GitHub 仓库

1. 启动页点击 **Clone repo**；
2. 在搜索栏粘贴仓库 URL，点击 **Clone from URL**；
3. 或点击 **Clone from GitHub**，授权后浏览自己的仓库；
4. 克隆完成后自动打开项目。

### 5.5 认识「信任工作区」

- 打开他人/远程项目时，IDE 可能提示是否信任该工作区；
- 信任后才能加载项目级 Skills、Rules、Hooks 等配置；
- 陌生仓库建议先不信任，或用只读方式检查。

### 5.6 本章小结

- [ ] 完成 Qoder IDE 安装与登录
- [ ] 会打开本地项目（Ctrl/Cmd + O）与克隆仓库
- [ ] 理解信任工作区的含义
# 第二部分 用 Qoder 写代码

## 第6章 安装 Qoder CLI：把 AI 带进终端

### 6.1 什么是 Qoder CLI

Qoder CLI（命令 `qodercli`）是终端版 AI 编程助手：你用自然语言描述需求，它自主读取文件、调用工具、执行命令，并在关键操作前按权限确认。适合熟悉终端的开发者，也适合 CI/CD 自动化。

### 6.2 安装方式

任选一种：

```bash
# 通过 npm 全局安装
npm install -g @qoder/cli

# 或使用 bun
bun add -g @qoder/cli

# 或使用 Homebrew（macOS）
brew install qoder/tap/qodercli
```

> 具体包名以官网 CLI 安装文档为准，安装后运行 `qodercli --version` 验证。

### 6.3 首次运行与登录

```bash
cd /path/to/your-project
qodercli
```

- 首次运行会自动引导登录（浏览器授权）；
- 看到对话提示符 `>` 后，即可用自然语言下指令；
- 非交互环境（CI）使用个人访问令牌认证：

```bash
export QODER_PERSONAL_ACCESS_TOKEN="你的令牌"
```

### 6.4 常用斜杠命令速查

| 命令 | 作用 |
| --- | --- |
| `!` | Bash 模式，直接运行 shell 命令 |
| `/help` | 查看 TUI 帮助 |
| `/status` | 查看版本、模型、账号与 API 连通性 |
| `/usage` | 查看 Credits 消耗 |
| `/resume` | 查看并恢复历史会话 |
| `/init` | 在项目中生成 AGENTS.md 记忆文件 |
| `/memory` | 查看/管理记忆 |
| `/diff`、`/review` | 审查本地代码改动 |
| `/quit` | 退出 |

### 6.5 升级与卸载

```bash
npm update -g @qoder/cli     # 升级
npm uninstall -g @qoder/cli  # 卸载
```

### 6.6 本章小结

- [ ] 完成 CLI 安装并 `qodercli --version` 验证
- [ ] 完成首次登录，能进入 TUI 对话
- [ ] 记住 `/status`、`/usage`、`/resume` 三个常用命令

## 第7章 认识 Qoder IDE 界面与快捷键

### 7.1 界面布局

- **左侧活动栏**：文件资源管理器、搜索、源代码管理（Git）、扩展等；
- **编辑器区**：多标签页打开代码文件，是 NEXT 与行间会话的主战场；
- **右侧 Chat 面板**：Ask / Agent 对话，快捷键 `Ctrl + L`；
- **底部状态栏**：分支、模型、账号、Credits 信息；
- **Quest 视窗**：独立窗口，用于长程任务委派。

### 7.2 四大 AI 入口对比

| 入口 | 快捷键 | 定位 |
| --- | --- | --- |
| NEXT | `Alt + P` | 光标处下一条编辑建议 |
| 行间会话 | `Ctrl + I` | 选中代码就地修改 |
| Chat 面板 | `Ctrl + L` | Ask 问答 / Agent 多步任务 |
| Quest 视窗 | 面板按钮 | 长程任务自主执行 |

### 7.3 高频快捷键总表

| 操作 | Windows/Linux | macOS |
| --- | --- | --- |
| 登录/设置 | Ctrl + Shift + , | Cmd + Shift + , |
| 打开项目 | Ctrl + O | Cmd + O |
| NEXT 建议 | Alt + P | Option + P |
| 行间会话 | Ctrl + I | Cmd + I |
| 应用行间代码 | Ctrl + Enter | Cmd + Enter |
| 打开 Chat | Ctrl + L | Cmd + L |

### 7.4 自定义快捷键

- 打开设置 → 键盘快捷方式，可搜索并修改以上快捷键；
- 与 VSCode 用户习惯一致，VSCode 键位可整套迁移。

### 7.5 本章小结

- [ ] 能说出四大 AI 入口及各自定位
- [ ] 熟记 Ctrl/Cmd + L（Chat）与 Ctrl/Cmd + I（行间）
- [ ] 会查看并修改快捷键

## 第8章 产品矩阵：IDE / CLI / 插件 / QoderWork / Cloud Agents

### 8.1 全景图

| 产品 | 形态 | 适合谁 |
| --- | --- | --- |
| Qoder IDE | 独立图形编辑器 | 新手首选、可视化开发 |
| Qoder CLI | 终端命令行 | 终端重度用户、自动化脚本 |
| JetBrains 插件 | IDEA / PyCharm / WebStorm 等 | Java/Python 生态开发者 |
| QoderWork | 本地桌面办公助手 | 办公文档、日常自动化 |
| Qoder Cloud Agents | 云端 Agent 平台 | 挂载 GitHub 仓库自动提 PR |
| QoderWake | 桌面助手（国内版） | 快速唤起与日常任务 |

### 8.2 JetBrains 插件

- 在 IDEA/PyCharm/WebStorm 插件市场搜索 Qoder 安装；
- 核心功能：智能补全、行间对话、Agent 模式；
- 与 IDE 原生重构、调试无缝配合。

### 8.3 QoderWork

- 面向日常办公的本地桌面助手；
- 可在设置面板用自然语言描述需求，自动生成 Skill 文件；
- 适合把重复性文档/数据处理流程沉淀为技能。

### 8.4 Qoder Cloud Agents

- 云端平台，直接挂载 GitHub 仓库；
- 让 Agent 自主修改代码并提交 Pull Request；
- 适合自动化维护、批量化改造、夜间任务。

### 8.5 如何组合使用

- **推荐组合**：IDE 写代码 + CLI 做自动化 + Quest 处理长任务；
- 团队标准化：规则与 Skills 通过 Git 共享，各形态行为一致。

### 8.6 本章小结

- [ ] 了解 Qoder 的六种产品形态
- [ ] 能说出 JetBrains 插件与 QoderWork 的适用人群
- [ ] 规划自己的产品组合

## 第9章 第一个对话：Ask 模式上手

### 9.1 打开 Chat 面板

按 `Ctrl + L`（macOS `Cmd + L`）打开右侧 Chat 面板，顶部工具栏可以选择 **Ask** 或 **Agent**。

### 9.2 Ask 模式是什么

- **Ask 以问答为主**：解释代码、查文档、讨论方案，不修改文件；
- 适合：理解代码、学习 API、排查思路、写注释说明。

### 9.3 第一个提问

打开一个项目，输入：

```
帮我梳理这个项目的整体架构和主要模块
```

- Qoder 会自主搜索并读取相关文件，给出带文件位置的解释；
- 可以顺着回答继续追问：「用户登录逻辑在哪个文件？」「这个模块的依赖关系是什么？」。

### 9.4 给 AI 提供上下文

- 在输入框中输入 `@`，选择要引用的文件，让 AI 聚焦分析；
- 选中编辑器中的代码再提问，AI 默认以选中内容为上下文；
- 问题越具体，答案越准：问「X 函数为什么这样写」优于「这段代码什么意思」。

### 9.5 Ask vs Agent 的选择

- 只想知道答案 → **Ask**；
- 需要改代码、跑命令 → **Agent**；
- 不确定时先用 Ask 问方案，确认后再切 Agent 执行。

### 9.6 本章小结

- [ ] 会用 Ctrl/Cmd + L 打开 Chat
- [ ] 完成一次「梳理项目架构」的提问
- [ ] 学会用 @ 引用文件补充上下文

## 第10章 NEXT：下一条编辑建议

### 10.1 NEXT 是什么

NEXT（Next Edit Suggestion）在光标处提供基于上下文的编辑建议，是 Qoder 的「实时补全」入口，支持多行编辑与流畅补全。

### 10.2 使用步骤

1. 在代码中输入部分代码，或用注释写自然语言意图，例如：

```ts
// 初始化一个 list
```

2. 按 `Alt + P`（macOS `Option + P`）唤出建议；
3. 按 `Tab` 接受当前建议，按 `Esc` 拒绝并跳过。

### 10.3 让 NEXT 更聪明

- **先写意图注释**：注释写清楚要做什么，NEXT 补全质量显著提升；
- **保持代码风格**：NEXT 会学习文件内的命名与风格；
- **多行场景**：接受首行后可以继续 `Alt + P` 逐段推进。

### 10.4 与 Tab 补全的区别

- 传统补全只猜下一个 token；
- NEXT 理解整个文件上下文，能一次补出完整函数、测试、配置片段。

### 10.5 常见问题

- 没有建议？检查是否已登录、光标是否在代码/注释后；
- 建议不对？按 Esc 后再补一个更明确的注释重新唤起；
- 太啰嗦？在设置中调整建议长度与触发时机。

### 10.6 本章小结

- [ ] 会用 Alt/Option + P 唤出 NEXT
- [ ] 会用注释描述意图提升补全质量
- [ ] 掌握 Tab 接受、Esc 拒绝的操作
## 第11章 行间会话：选中代码就地修改

### 11.1 什么是行间会话

行间会话（Inline Chat）让你在代码上下文中直接获得 AI 帮助——选中代码，按快捷键，AI 就地给出修改建议，确认后一键应用。

### 11.2 基本操作

1. 在编辑器中选中一段代码，按 `Ctrl + I`（macOS `Cmd + I`）打开行间会话；
2. 输入请求并发送，例如：「新增一个文件上传处理函数」「把这个循环改成流式写法」；
3. 按 `Ctrl + Enter`（macOS `Cmd + Enter`）应用生成代码；
4. 不想要则直接关闭输入框（Esc），不会改动文件。

### 11.3 典型场景

| 场景 | 示例指令 |
| --- | --- |
| 加注释 | 「给这个函数补中文注释」 |
| 修 Bug | 「这个函数在空数组时会报错，修复它」 |
| 重构 | 「把这段重复逻辑抽取成工具函数」 |
| 改风格 | 「改成 Kebab-case 命名并整理格式」 |
| 补边界 | 「给这个函数加上参数校验」 |

### 11.4 进阶技巧

- **多轮迭代**：应用后继续 `Ctrl + I` 对结果提新要求；
- **跨文件引用**：在行间会话中用 `@文件` 引入其他文件作为参考；
- **与 Agent 联动**：改动较大时，把需求交给 Agent 模式在多个文件间完成。

### 11.5 与 Copilot 类工具的差异

- 行间会话是「选中即改」，比纯补全更主动；
- 所有修改都经过你确认，不会静默改动代码。

### 11.6 本章小结

- [ ] 会用 Ctrl/Cmd + I 打开行间会话
- [ ] 会用 Ctrl/Cmd + Enter 应用修改
- [ ] 掌握三类典型指令：注释、修 Bug、重构

## 第12章 Agent 模式：让 AI 动手干活

### 12.1 什么是 Agent 模式

Agent 是 Chat 面板中的智能体模式：你描述目标，它自主读取文件、修改代码、运行命令，并在关键操作前请求确认。适合多文件修改与功能实现。

### 12.2 打开方式

- `Ctrl + L` 打开 Chat 面板，顶部工具栏切换到 **Agent**；
- 输入任务描述，例如：「为这个函数创建并运行测试」。

### 12.3 一个完整流程示例

1. 输入：「给 `src/utils/date.ts` 的 `formatDate` 函数补单元测试并运行验证」；
2. Agent 会：定位函数 → 查看现有测试框架 → 编写测试文件 → 运行测试 → 反馈结果；
3. 中途请求确认时，点允许或拒绝；
4. 完成后按提示应用变更（或让 Agent 直接写入）。

### 12.4 观察与干预

- 对话流中可以看到 Agent 的**思考过程与工具调用**（读了哪些文件、跑了什么命令）；
- 发现方向偏了，随时插入新指令纠正，不必中断；
- 高风险命令（如删除文件）默认需要你确认。

### 12.5 Agent 任务描述模板

```
目标：<要实现的最终效果>
范围：<涉及的文件/模块>
约束：<技术栈、命名、性能要求>
验收：<如何验证成功，如运行某条测试>
```

### 12.6 本章小结

- [ ] 能在 Chat 面板切换到 Agent 模式
- [ ] 完成一次「补测试并运行」的 Agent 任务
- [ ] 学会在任务中加入目标/约束/验收

## 第13章 Quest 模式：长程任务自主委派

### 13.1 什么时候用 Quest

当任务**长程、多步、跨文件**时（比如「从零实现一个用户管理模块」），用 Quest 独立视窗把任务委派给 Agent 持续推进，你可以在同一工作台查看进度与产物。

### 13.2 打开与发起

1. 点击界面上的 **Quest** 入口打开独立视窗；
2. 输入一句自然语言需求；
3. Agent 生成 **Spec（需求规格）** 与实施计划，可先审阅确认；
4. 确认后 Agent 开始执行，视窗中显示看板、进度与交付产物。

### 13.3 执行过程中的控制

- **看板**：查看任务拆解与完成状态；
- **暂停/继续**：随时暂停任务，补充需求后再继续；
- **审查**：Agent 产生的代码改动会进入 Review 面板（见第21章）；
- **完成清单**：Quest 结束时给出总结清单，逐项确认。

### 13.4 Quest vs Agent 模式

| 维度 | Chat Agent | Quest |
| --- | --- | --- |
| 定位 | 对话内的多步任务 | 独立视窗的长程委派 |
| Spec 确认 | 无 | 有，先出方案再执行 |
| 进度追踪 | 对话流 | 看板 + 任务列表 |
| 典型任务 | 补测试、改接口 | 整模块开发、系统改造 |

### 13.5 最佳实践

- 需求一次说清目标与验收，减少中途返工；
- 大任务拆成几个 Quest 分阶段推进；
- 每个 Quest 结束用 Review 面板审查 diff 后再提交。

### 13.6 本章小结

- [ ] 会打开 Quest 视窗并发起任务
- [ ] 理解 Spec 确认的作用
- [ ] 会用看板追踪进度、Review 面板审查产出

## 第14章 Experts 专家团：多智能体并行协作

### 14.1 一句话理解

你提需求，AI 专家团帮你交付结果。Lead Agent 作为「大脑」拆解任务、动态拉起不同领域的专家子智能体并行工作。

### 14.2 专家角色表

| 角色 | 职责 |
| --- | --- |
| Lead Agent | 理解需求、拆解任务、协调调度、质量把关 |
| 调研员 | 代码定位、依赖梳理、环境检查、报告输出 |
| 全栈工程师 | 前后端及跨栈代码实现 |
| QA | 运行测试构建、收集验证结果 |
| 代码审查员 | 代码审查、风险识别与改进建议 |
| UI 操作者 | 浏览器与 UI 端到端验证、可视化 Bug 复现 |
| 故障诊断工程师 | 故障复现、根因定位、修复建议 |

### 14.3 如何启用

1. 在 Quest 输入区底部的模式切换按钮中选择**专家团（Experts）模式**；
2. 用自然语言描述需求（目标、技术栈偏好、质量要求）；
3. Lead Agent 先生成实施方案，确认后专家团队并行开工；
4. 会话流中可看到每个专家执行状态，点击查看详情。

### 14.4 适用场景

- 前后端全栈开发（用户管理、支付模块等）；
- 复杂问题定位（线上性能瓶颈、多服务排查）；
- 技术方案调研（如评估 GraphQL 替代 REST）。

### 14.5 成本与注意

- 官方内部测试称质量提升约 67%，但耗时与 credits 消耗更高；
- 简单任务用单 Agent 更高效；
- 专家模式中终端命令自动运行，危险命令自动进沙箱执行。

### 14.6 本章小结

- [ ] 能在 Quest 中切换到 Experts 模式
- [ ] 说出至少 4 个专家角色
- [ ] 知道简单任务不必动用专家团

## 第15章 从零新建项目：自然语言驱动

### 15.1 思路

Qoder 可以把「从零搭建一个 Web 应用」这类需求直接变成可运行代码。你只需要描述清楚技术栈与功能。

### 15.2 示例：用 Agent 从零搭一个待办应用

```
帮我用 Bun + TypeScript + Hono 新建一个 TODO 应用：
- 支持增删改查，数据存本地 JSON 文件
- REST API，提供接口文档注释
- 附带冒烟测试
完成后运行 bun dev 并告诉我访问地址
```

### 15.3 执行流程

1. Agent 规划目录结构与依赖；
2. 逐文件生成代码（路由、数据层、测试）；
3. 安装依赖并启动服务验证；
4. 把访问地址与使用说明交给你。

### 15.4 描述需求的三要素

- **技术栈**：语言、框架、包管理器（Bun/npm/pnpm）、数据库；
- **功能清单**：要做什么，每项尽量具体；
- **质量标准**：要测试吗？要文档吗？代码风格？

### 15.5 生成后的动作

- 用 Review 面板检查生成的每个文件；
- 手动运行测试确认可跑；
- 让 Qoder 补充 README 与提交信息，完成首次提交。

### 15.6 本章小结

- [ ] 会描述「技术栈 + 功能 + 质量」三要素
- [ ] 完成一次从零新建项目
- [ ] 养成生成后先审查再提交的习惯
## 第16章 快速读懂陌生代码库

### 16.1 目标

接手旧项目或开源项目时，用自然语言让 Qoder 带你快速建立全局认知，省去逐文件翻读的时间。

### 16.2 第一步：问整体

```
帮我梳理这个项目的整体架构和主要模块
```

- Qoder 会自主搜索、读取关键文件，给出带文件位置的解释；
- 顺着回答追问：「入口文件在哪」「数据流是怎么走的」。

### 16.3 第二步：生成 AGENTS.md

输入 `/init`，Qoder 会分析项目并生成初始项目说明文件 `AGENTS.md`，之后每次会话它都会自动加载这份项目约定，越用越懂你的项目。

### 16.4 第三步：深入模块

| 问题类型 | 示例 |
| --- | --- |
| 定位 | 「用户登录校验逻辑在哪个文件？」 |
| 关系 | 「orders 模块依赖哪些模块？」 |
| 调用链 | 「从 HTTP 请求到数据库的完整调用链」 |
| 配置 | 「数据库连接配置在哪里，怎么改」 |

### 16.5 让知识沉淀下来

- 把梳理清楚的关键结论写进 `AGENTS.md` 或项目文档；
- 大型项目可以把子系统知识整理成**结构化知识模块**（按模块拆分的说明页），Agent 按需检索；
- Repo Wiki 功能会自动沉淀知识，生成项目文档与架构图谱。

### 16.6 本章小结

- [ ] 会问「整体架构」类问题快速入门
- [ ] 会用 /init 生成 AGENTS.md
- [ ] 会把关键知识写回文档，让 AI 越用越懂项目

## 第17章 重构与批量修改

### 17.1 用自然语言描述重构

把重构目标讲清楚，而不是只给操作指令：

```
把 src/ 下所有 API handler 中重复的错误处理代码抽取到
src/api/errorHandler.ts，保持行为不变，不改变对外接口，
完成后运行全部测试验证
```

### 17.2 批量修改示例

- 「把所有 `user_id` 字段改名为 `userId`，同步更新类型与测试」；
- 「将项目内 console.log 全部替换为 logger.info，并补充上下文参数」；
- 「把 utils 里的日期处理统一改用 dayjs」。

### 17.3 重构的安全姿势

1. **先有测试**：重构前确保测试覆盖，或让 Qoder 先补关键测试；
2. **小步提交**：一次重构一个主题，改完立刻提交；
3. **行为不变**：在需求里强调「不改变对外接口与行为」；
4. **对比 diff**：用 Review 面板逐文件核对改动。

### 17.4 跨文件一致性

- Agent 模式天然跨文件工作，适合「同一约定全局生效」类任务；
- 把项目规范写进 Rules（见第40章），Agent 会自觉遵守。

### 17.5 本章小结

- [ ] 会描述「目标 + 范围 + 验证方式」的重构需求
- [ ] 养成先补测试、小步提交的习惯
- [ ] 会用 Review 面板核对批量改动

## 第18章 让 Qoder 写单元测试

### 18.1 基本用法

选择目标函数，打开行间会话或 Agent 模式：

```
给 formatDate 函数补单元测试，覆盖正常输入、空值、边界日期，
用项目现有的 vitest 框架，完成后运行 bun test 验证
```

### 18.2 提升测试质量的关键

- **指定框架**：vitest / jest / pytest / go test，避免 AI 猜错；
- **指定覆盖点**：正常、边界、异常三类的具体例子；
- **要求可运行**：让 Qoder 直接运行验证，而不是只生成文件。

### 18.3 测试驱动开发（TDD）

```
先为 parseCSV 编写测试（此时应失败），
再实现 parseCSV 让测试通过，最后补充边界用例
```

- Qoder 可以按 TDD 节奏一步步执行；
- 每步都运行测试，确认红→绿过程。

### 18.4 让测试成为习惯

- 在 `AGENTS.md` 里写：「提交前运行 `bun test`」；
- 配置 Hook：写文件后自动跑 lint/测试（见第34章）；
- Quest 完成清单里加入「所有测试通过」验收项。

### 18.5 本章小结

- [ ] 会指定框架让 Qoder 生成可运行测试
- [ ] 会用 TDD 三步法驱动实现
- [ ] 会把「测试通过」写进项目约定与验收标准

# 第三部分 Git 与版本管理

## 第19章 Git 基础：从零理解版本管理

### 19.1 为什么需要 Git

Git 是团队协作与个人开发的版本控制系统：记录每次修改、支持回滚、多人并行。Qoder 的 AI 能力可以帮你把 Git 操作从「记命令」变成「说需求」。

### 19.2 四个核心概念

- **工作区（Working Directory）**：你正在编辑的文件；
- **暂存区（Staging Area）**：准备提交的改动清单；
- **提交（Commit）**：把暂存内容固化为一个版本快照；
- **推送（Push）**：把本地提交同步到远程仓库（GitHub 等）。

### 19.3 基础命令速查

```bash
git status              # 查看当前状态
git add <file>          # 暂存文件
git commit -m "msg"     # 提交
git push                # 推送到远程
git pull                # 拉取远程更新
git log --oneline       # 查看提交历史
```

### 19.4 交给 Qoder 的部分

- 生成 `.gitignore`：「帮我生成 Node.js 项目的 .gitignore」；
- 写提交信息：「生成本次改动的提交信息」；
- 解释报错：「git 说 there is no tracking information，怎么办」。

### 19.5 在 IDE 中查看

- 左侧「源代码管理」面板可看到所有改动文件与 diff；
- 未提交的改动会显示标记，点击即可查看具体差异。

### 19.6 本章小结

- [ ] 理解工作区/暂存区/提交/推送四个概念
- [ ] 掌握 git status / add / commit / push 基础命令
- [ ] 会让 Qoder 生成 .gitignore 与提交信息

## 第20章 在 IDE 里管理 Git：可视化操作

### 20.1 源代码管理面板

- 点击左侧 Git 图标（分支图标）打开；
- 顶部显示当前分支、改动文件数；
- 列表按「更改 / 暂存的更改」分组。

### 20.2 日常流程（可视化版）

1. 改动代码后，在面板查看所有变更文件；
2. 点击文件查看 diff，红色为删除、绿色为新增；
3. 点击文件旁的 + 号暂存（Stage），- 号取消暂存；
4. 输入提交信息，点击 Commit；
5. 点击 Push 推送，或打开「同步」按钮拉取+推送。

### 20.3 让 Qoder 帮忙处理

在 Chat 中输入：

```
把今天的改动整理成规范的提交信息并提交，注意只提交 src/ 下的文件
```

- Qoder 会读取 diff、按规范生成信息并执行提交；
- 关键操作前会请求确认，安全性可控。

### 20.4 解决冲突

- 拉取/合并出现冲突时，IDE 会标记冲突文件；
- 让 Qoder 处理：「帮我解决 merge 冲突，保留两边的关键逻辑，跑测试验证」。

### 20.5 本章小结

- [ ] 会在 IDE 中查看 diff、暂存、提交、推送
- [ ] 会让 Qoder 生成提交信息并提交
- [ ] 知道冲突文件长什么样，能交给 AI 处理
## 第21章 Quest Review 面板：审查、暂存、提交、推送

### 21.1 Review 面板是什么

每当 Qoder 在 Quest 中产生代码改动，右侧 **Review 面板**会完整展示 Diff，让你决定保留还是丢弃，并完成 Git 提交与推送。

### 21.2 三个区域

- **顶部操作栏**：查看范围切换、Stage All / Discard All、提交按钮；
- **文件列表**：当前范围内变更文件，支持列表/树视图与搜索；
- **Diff 视图**：单文件行级对比，附带单文件 Stage / Discard 操作。

### 21.3 查看范围切换

| 选项 | 含义 |
| --- | --- |
| Current Quest | 当前 Quest 累计的全部变更 |
| Last Turn | 最近一轮对话产生的变更 |
| All Uncommitted | 工作区所有未提交变更（等价 git status） |

### 21.4 在 Diff 中批注

- 审查时可直接在代码/Diff 中**添加行内批注**；
- 批注会直接反馈给智能体，它就能准确理解「哪一处、要怎么改」；
- 比用文字描述位置高效得多。

### 21.5 暂存与丢弃

- 全局：Stage All / Discard All（Discard 需二次确认，不可恢复）；
- 单文件：未暂存显示 Stage/Discard，已暂存显示 Unstage/Discard；
- 注意：Hunk（局部）级暂存暂不支持。

### 21.6 提交与推送

- 顶部操作按钮默认为 **Commit**，下拉可切换 **Push** 与 **Create New Branch**；
- Commit 弹窗可手动填写或**自动生成提交信息**；
- 从不同范围提交时，只提交该范围内的文件。

### 21.7 本章小结

- [ ] 会用 Review 面板查看 Quest 的变更范围
- [ ] 会在 Diff 中加行内批注驱动修改
- [ ] 会完成 Stage / Commit / Push / 新建分支

## 第22章 CLI 中的 Git 工作流

### 22.1 日常循环

Qoder CLI 覆盖「探索代码 → 修改 → 测试 → 审查 → 提交 → 推送」全流程，全程用自然语言驱动。

### 22.2 审查改动

在 TUI 中输入：

```
/diff
```

- 查看当前所有改动的差异；
- 进一步结构化审查：

```
/review
```

- Qoder 会对本地代码改动做质量、安全、性能维度的审查并给出建议；
- 两个命令都需在 Git 仓库中使用。

### 22.3 提交与推送

```
帮我生成提交信息并提交这次改动
```

- Qoder 读取 diff 生成规范提交信息；
- 确认后执行 git commit；需要推送时追加：

```
提交后推送到 origin 的 main 分支
```

### 22.4 直接跑 Git 命令

- 输入 `!` 进入 Bash 模式，直接运行 `!git log --oneline` 查看历史；
- 复杂场景（rebase、cherry-pick）可先让 Qoder 解释，再决定是否执行。

### 22.5 常见任务提示词

- 「看看有哪些未提交改动，并解释每处改动的意图」；
- 「撤销最近一次提交但保留工作区改动」；
- 「对比 main 与 feature-x 分支的差异」。

### 22.6 本章小结

- [ ] 会用 /diff 与 /review 审查改动
- [ ] 会用自然语言完成提交与推送
- [ ] 会使用 ! 进入 Bash 模式执行 git 命令

## 第23章 分支、合并与 Pull Request

### 23.1 基于变更新建分支

在 Quest Review 面板：

1. 展开提交按钮下拉，选择 **Create New Branch**；
2. 输入分支名（如 `feature/login`）；
3. 基于当前变更创建分支并提交。

### 23.2 CLI 中的分支操作

```bash
!git checkout -b feature/login   # 新建并切换分支
!git push -u origin feature/login # 推送并建立跟踪
```

### 23.3 让 Qoder 处理 PR

在 CLI 或 IDE 中输入：

```
帮我为当前分支创建 Pull Request，写好标题和描述，说明改动内容与测试情况
```

- Qoder 读取 diff 自动生成 PR 描述；
- 配合 GitHub CLI（`gh`）可完成创建与合并；
- 处理别人的 PR：「审查这个 PR 的改动，列出问题与改进建议」。

### 23.4 合并与冲突

- 合并前先让 Qoder 审查目标分支的 diff；
- 冲突时交给 Qoder：「解决 main 合入时的冲突，保留双方语义，跑测试验证」；
- 合并策略（merge / squash / rebase）写进团队规范。

### 23.5 本章小结

- [ ] 会用 Review 面板一键创建分支
- [ ] 会让 Qoder 生成 PR 描述
- [ ] 会委托 AI 处理冲突与合并

## 第24章 提交信息规范与 Git 自动化

### 24.1 Conventional Commits 规范

```
<type>(<scope>): <subject>

feat(user): 新增登录接口
fix(api): 修复空数组导致的 500
docs(readme): 补充安装说明
test(date): 增加边界用例
refactor(utils): 抽取错误处理
```

- type：feat / fix / docs / test / refactor / chore 等；
- scope：影响模块，可选；subject：一句话描述，动词开头。

### 24.2 让 Qoder 按规范提交

把规范写进项目规则（AGENTS.md 或 rules）：

```
# 提交规范
- 使用 Conventional Commits 格式
- subject 不超过 50 字符，动词开头
- 有破坏性变更时在正文说明 BREAKING CHANGE
```

之后只需说「提交这次改动」，Qoder 就会自动按规范生成。

### 24.3 用 Hooks 强制检查

配置 Hook：提交前检查提交信息是否符合规范，不符合则阻止（具体写法见第34章）。

### 24.4 CI 中的提交门禁

- 在 GitHub Actions 中加入 commitlint 校验 PR 标题与提交信息；
- Qoder 生成的信息天然符合规范，CI 不再红。

### 24.5 本章小结

- [ ] 掌握 Conventional Commits 六种常用 type
- [ ] 把提交规范写进 AGENTS.md
- [ ] 会用 Hook / CI 强制提交规范

# 第四部分 Skills 与扩展

## 第25章 Skills 入门：把专业知识打包成技能

### 25.1 Skills 是什么

Skills 是 Qoder 将**专业知识打包成可复用功能**的机制。每个 Skill 包含一个 `SKILL.md` 文件，定义技能的描述、指令和可选辅助文件。IDE 与 CLI 中用法完全一致。

### 25.2 适合的场景

- 复杂专业任务：代码审查、PDF 处理、API 设计；
- 标准化流程：提交规范、部署流程；
- 团队知识共享：打包最佳实践供团队使用；
- 重复性工作：频繁执行且需要专业指导的任务。

### 25.3 内置 Skills 一览

在会话或 Quest 中输入 `/` 可看到（示例）：

| 命令 | 用途 |
| --- | --- |
| /create-skill | 引导创建新的 Skill |
| /create-skill-ui | 为 Skill 生成交互式 HTML Widget |
| /vercel-deploy | 一键 Vercel 部署 |
| /create-subagent | 脚手架自定义子智能体 |
| /canvas | 创建/编辑 .canvas.tsx 视觉产物 |

### 25.4 两种触发方式

- **自动触发**：直接描述需求，模型自动判断是否调用：

```
分析这个日志文件中的错误
```

模型会自动调用 log-analyzer Skill。

- **手动触发**：输入 `/skill-name` 显式调用：

```
/log-analyzer
```

### 25.5 为什么用 Skills 而不是写长提示词

- 提示词每次都要重写，Skills 一次封装反复复用；
- Skill 可携带脚本、模板等辅助文件；
- 可通过 Git 与团队共享，标准化流程。

### 25.6 本章小结

- [ ] 理解 Skills = SKILL.md 打包的知识技能
- [ ] 说出至少 3 个内置 Skill
- [ ] 掌握自动触发与 /name 手动触发两种方式
## 第26章 安装社区 Skills：一行命令装技能

### 26.1 两种来源

- **skills.sh 市场**：社区共享的成熟技能，一键安装；
- **GitHub 仓库**：从任意仓库安装指定技能。

### 26.2 使用 skills CLI 安装

在 Qoder IDE 的终端内执行：

```bash
# 从 skills.sh 市场安装
npx skills add vercel-labs/agent-browser -a qoder

# 从 GitHub 仓库安装指定技能
npx skills add https://github.com/anthropics/skills --skill skill-creator -a qoder
```

- `-a qoder` 表示安装到 Qoder 的 skills 目录；
- 更多用法见 skills CLI 官方文档（github.com/vercel-labs/skills）。

### 26.3 验证安装

- 重启 Qoder IDE（或刷新会话）；
- 在对话框输入 `/`，查看已加载的 Skills 列表；
- 用第25章的方式测试自动/手动触发。

### 26.4 更新与移除

- 重新执行 `npx skills add` 可覆盖更新；
- 直接删除对应技能目录即可移除：
  - 用户级：`~/.qoder/skills/<技能名>/`
  - 项目级：`.qoder/skills/<技能名>/`

### 26.5 安全提醒

- 只安装可信来源（官方、知名团队）的技能；
- 安装前先看 SKILL.md 内容，尤其注意其中的脚本会不会执行危险命令；
- 项目级技能会随仓库传播，团队仓库内要审查。

### 26.6 本章小结

- [ ] 会用 npx skills add 安装市场/仓库技能
- [ ] 会验证与移除技能
- [ ] 养成安装前审查技能内容的习惯

## 第27章 手写 SKILL.md：完全自定义你的技能

### 27.1 目录结构

一个 Skill 就是一个目录 + SKILL.md（可带辅助文件）：

```
log-analyzer/
├── SKILL.md        # 技能定义（必需）
└── scripts/
    └── parse.py    # 可选辅助脚本
```

### 27.2 存放位置

| 作用域 | 路径 | 生效范围 |
| --- | --- | --- |
| 用户级 | `~/.qoder/skills/{skill-name}/SKILL.md` | 当前用户所有项目 |
| 项目级 | `.qoder/skills/{skill-name}/SKILL.md` | 仅当前项目 |

> 同名 Skill 存在时，**项目级优先**。

### 27.3 SKILL.md 内容结构

```markdown
---
name: log-analyzer
description: 分析日志文件中的错误、性能问题与异常模式。当用户说"分析日志"时使用。
---

# 日志分析

## 步骤
1. 读取日志文件，统计 ERROR / WARN 级别记录
2. 提取时间戳、模块、错误信息
3. 按错误类型分组并给出根因建议

## 输出格式
- 错误摘要（Top 5）
- 每条错误的：时间范围、出现次数、示例、建议
```

- `name`：技能名，也是 `/name` 触发词；
- `description`：模型自动判断是否使用的依据，写清楚**触发场景**。

### 27.4 一个完整的「代码审查」Skill 示例

```markdown
---
name: code-reviewer
description: 按团队规范审查代码。当用户要求"审查代码/审查改动"时使用。
---

# 代码审查规则

1. 检查点：正确性、安全性、性能、可读性、测试覆盖
2. 每个问题给出：位置、严重级别（P0/P1/P2）、修改建议
3. 只报告有依据的问题，不臆测
4. 输出 Markdown 审查报告，附文件路径与行号
```

### 27.5 编写技巧

- description 决定自动触发率，写「当用户说 X 时使用」；
- 指令用编号步骤，明确「先做什么、输出什么格式」；
- 辅助文件（脚本/模板）与 SKILL.md 放同一目录，用相对路径引用。

### 27.6 本章小结

- [ ] 会创建用户级/项目级技能目录
- [ ] 会写 name + description + 步骤/格式的 SKILL.md
- [ ] 理解同名技能时项目级优先

## 第28章 用 /create-skill 交互式创建技能

### 28.1 为什么用它

手写 SKILL.md 需要了解格式，`/create-skill` 通过交互式对话引导你一步步生成规范文件，无需记住细节。

### 28.2 使用方法

在会话或 Quest 中输入：

```
/create-skill 将 Word 文档转换为 PDF
```

- Qoder 会引导你补充：技能名称、触发场景、执行步骤、输出要求；
- 交互完成后生成 SKILL.md 与目录结构；
- 可以选择保存到用户级还是项目级。

### 28.3 创建后的检查清单

1. 打开生成的 SKILL.md，确认 name/description 符合预期；
2. 补充缺失的边界情况与示例；
3. 测试触发：说一句符合 description 的话，看是否自动调用；
4. 效果不佳就迭代描述与步骤。

### 28.4 在 QoderWork 中创建

- QoderWork 设置面板可以直接用自然语言描述需求，由它自动生成 Skill 文件；
- 适合不熟悉文件格式的办公用户。

### 28.5 本章小结

- [ ] 会用 /create-skill 交互式创建技能
- [ ] 会检查并测试新技能
- [ ] 知道 QoderWork 也能生成 Skill

## 第29章 Skill UI 与一键部署 /vercel-deploy

### 29.1 Skill UI：让技能带界面

Skill UI 支持 Agent 在执行过程中直接渲染**可交互 HTML 组件**（表单、图表、配置面板），组件内嵌在对话流中，无需跳转外部页面。

### 29.2 创建 Skill UI

1. 在 Quest 的 Agent 模式中使用 `/create-skill-ui` 命令；
2. Agent 为指定 Skill 创建 HTML Widget 界面；
3. 通过实时预览迭代设计，满意后保存为模板文件。

### 29.3 适用场景

- 表单式配置面板（部署参数、环境变量）；
- 可视化操作（Canvas 预览、图表配置）；
- 需要用户输入交互的任务。

### 29.4 /vercel-deploy 一键部署

在 Quest 对话中执行：

```
/vercel-deploy
```

1. Qoder 自动启动部署工作流（CLI 安装、OAuth 授权、构建、上线）；
2. 首次部署引导完成 **Vercel OAuth 登录授权**；
3. 授权后自动构建并部署到 Vercel 生产环境；
4. 部署成功返回可访问的线上链接。

### 29.5 部署后的动作

- 打开线上链接验证功能；
- 让 Qoder 把部署信息写入 README 与提交信息；
- 配置环境变量、自定义域名等后续事项可继续对话处理。

### 29.6 本章小结

- [ ] 会用 /create-skill-ui 生成交互组件
- [ ] 会用 /vercel-deploy 完成一键部署
- [ ] 了解 OAuth 授权与自动构建流程

## 第30章 子智能体与 MCP 扩展

### 30.1 自定义子智能体（Subagents）

`/create-subagent` 可以脚手架一个自定义子智能体，适用于 Agent 与 Experts 模式，由主智能体按需调用。

- 可配置：名称、角色描述、指定模型；
- 可扩展：为子智能体附加 Skills 与 MCP 服务器；
- 场景：固定角色的专家（如「React 组件审查员」）。

### 30.2 MCP 是什么

MCP（Model Context Protocol）让 Qoder 接入外部服务与自定义工具：数据库、浏览器、内部系统等。CLI 与 IDE 均支持配置 MCP 服务器。

### 30.3 配置 MCP 服务器

以 CLI 为例（配置文件按官方格式编写）：

```json
{
  "mcpServers": {
    "my-db": {
      "command": "npx",
      "args": ["-y", "mcp-server-postgres", "--conn", "postgres://..."],
      "env": { "KEY": "value" }
    }
  }
}
```

### 30.4 MCP 安全要点

- 服务器自声明的 `readOnlyHint` / `destructiveHint` **不可信**；
- 用 `--allowed-tools` 白名单限制 MCP 工具可用范围；
- 危险工具（写库、删除）默认要求确认或直接拒绝。

### 30.5 让子智能体带 MCP

- 在自定义子智能体配置中勾选需要的 MCP 服务器；
- Experts 模式中可为每个专家单独配置 Skills 与 MCP；
- 场景示例：「数据库运维专家」挂只读查询 MCP。

### 30.6 本章小结

- [ ] 会用 /create-subagent 创建自定义智能体
- [ ] 会为 CLI/IDE 配置 MCP 服务器
- [ ] 牢记 MCP 权限要用白名单而不是信任声明
# 第五部分 代码审查

## 第31章 代码审查：Code Review Agent 总览

### 31.1 能审什么

Code Review Agent 是 Qoder Agent 模式的内置能力，可对以下对象自动审查：

- 整个项目；
- 特定文件；
- Git diff（未提交改动）；
- Pull Request。

审查维度覆盖**质量、安全、性能**，并给出可执行的修改建议。

### 31.2 怎么用

在 Chat（Agent 模式）中输入：

```
审查 src/utils/date.ts 这个文件，重点看边界处理与安全性
```

或审查整体改动：

```
审查当前未提交的改动，列出问题与修改建议
```

### 31.3 审查报告长什么样

- 问题列表：文件路径 + 行号 + 严重级别 + 说明 + 修改建议；
- 按严重级别排序（如 P0 阻断 / P1 应修 / P2 建议）；
- 关键问题附带示例代码修复方案。

### 31.4 与人类审查的关系

- AI 负责：遗漏扫描、风格一致、常见反模式、安全基线；
- 人类负责：架构权衡、业务语义、团队默契；
- 最佳实践：AI 先审一遍 → 人工聚焦 AI 报告的 P0/P1 与架构层面 → 循环直至通过。

### 31.5 让审查更贴合团队

- 把团队规范写进 Rules（第40章）与 code-reviewer Skill（第27章示例）；
- 指定审查重点：「重点看 SQL 注入与鉴权逻辑」；
- 要求输出格式：「输出 Markdown 表格，含位置/级别/建议」。

### 31.6 本章小结

- [ ] 知道 Code Review Agent 能审项目/文件/diff/PR
- [ ] 会发起一次文件级审查并读懂报告
- [ ] 规划好 AI 与人工审查的分工

## 第32章 审查 Git diff 与 PR：实操流程

### 32.1 CLI 审查流程

在 Git 仓库中：

```
/diff    # 先看改动
/review  # 结构化审查
```

- `/review` 会综合质量、安全、性能给出结构化报告；
- 看完后可直接让 Qoder 按建议修复：

```
按 review 的建议修复 P0 和 P1 问题，然后重新跑测试
```

### 32.2 在 Quest Review 面板批注

- 对具体行加**行内批注**，反馈直接进入智能体的上下文；
- 批注驱动的修改更精准，不用重复描述位置。

### 32.3 在 PR 上审查

- IDE/CLI 中：「审查 origin/feature-x 与 main 的差异」；
- 也可以让 Qoder 基于 GitHub 的 PR diff 输出审查意见，再人工贴到 PR。

### 32.4 Qoder Action：PR 自动审查

Qoder Action 是标准 GitHub Actions 组件，可在 PR/Issue 中直接进行 AI 代码协作。示例 workflow（.github/workflows/qoder-review.yml）：

```yaml
name: Qoder Review
on:
  pull_request:
    types: [opened, synchronize]
jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: AI Review
        uses: qoder/qoder-action@v1
        with:
          token: ${{ secrets.QODER_TOKEN }}
          mode: review
```

> 具体 action 名称与参数以官方仓库为准。

### 32.5 审查提示词模板

```
请审查以下改动：
1. 正确性：是否有逻辑错误或边界遗漏
2. 安全性：输入校验、注入、敏感信息
3. 性能：不必要的循环、N+1 查询
4. 可维护性：命名、重复代码、复杂度
输出：按严重级别排序的问题清单 + 修复建议
```

### 32.6 本章小结

- [ ] 掌握 /diff + /review 的 CLI 审查流程
- [ ] 会用行内批注驱动修改
- [ ] 会在 CI 中配置 Qoder Action 自动审查 PR

## 第33章 自定义审查规则：让 AI 按团队标准审查

### 33.1 为什么需要自定义

默认审查是通用基线；团队往往有自己的规范（命名、目录、错误处理约定、安全红线），把这些写下来，AI 才会按团队标准审查。

### 33.2 用 Rules 承载审查规范

在 `.qoder/rules/` 下建一个 `code-review.md`：

```markdown
---
trigger: manual
---
# 代码审查规范

- 所有外部输入必须做校验，禁止拼接 SQL
- 禁止把密钥/令牌硬编码进代码
- API 必须返回统一错误结构 { code, message }
- 新增公共函数必须有单元测试
- 优先函数式风格，避免深层嵌套
```

- `trigger: manual` 表示按需调用；
- 审查时输入：「按 code-review 规则审查本次改动」。

### 33.3 用 Skill 封装审查流程

参考第27章的 code-reviewer Skill，把「规则 + 输出格式」打包，团队共享一份。

### 33.4 严重级别约定

| 级别 | 含义 | 处理 |
| --- | --- | --- |
| P0 | 阻断（安全问题、数据损坏） | 合入前必须修复 |
| P1 | 应修（明显缺陷、规范违反） | 本次迭代修复 |
| P2 | 建议（可读性、优化） | 记录后续处理 |

### 33.5 迭代优化

- 每次审查后检查误报与漏报，更新规则；
- 把反复出现的问题沉淀进 rules 与 Skill，越审越准。

### 33.6 本章小结

- [ ] 会用 rules 文件承载团队审查规范
- [ ] 会定义 P0/P1/P2 严重级别
- [ ] 会持续迭代规则减少误报

## 第34章 用 Hooks 自动化质量门禁

### 34.1 Hooks 是什么

Hooks 允许在 Qoder 的关键节点介入 Agent 主流程，与 CLI 解耦，通过 JSON 配置即可生效，无需改代码。

### 34.2 配置位置

三个配置文件（会合并执行）：

```
~/.qoder/settings.json           # 用户级，所有项目生效
<项目>/.qoder/settings.json      # 项目级，可提交 git 共享
<项目>/.qoder/settings.local.json # 本地，建议加入 .gitignore
```

### 34.3 常用事件

| 事件 | 触发时机 | 典型用途 |
| --- | --- | --- |
| SessionStart | 会话开始 | 注入附加上下文 |
| UserPromptSubmit | 用户提交提示词 | 拦截危险提示 |
| PreToolUse | 工具执行前 | 拦截危险命令 |
| PostToolUse | 工具执行成功后 | 自动 lint / 通知 |
| Stop | Agent 停止时 | 检查未完成任务 |

### 34.4 示例：写文件后自动 Lint

脚本 `<项目>/.qoder/hooks/auto-lint.sh`：

```bash
#!/bin/bash
input=$(cat)
file_path=$(echo "$input" | jq -r '.tool_input.file_path')

case "$file_path" in
  *.js|*.ts|*.jsx|*.tsx)
    npx eslint "$file_path" --fix 2>/dev/null
    ;;
esac
exit 0
```

配置：

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          { "type": "command", "command": ".qoder/hooks/auto-lint.sh" }
        ]
      }
    ]
  }
}
```

### 34.5 示例：阻止危险命令

PreToolUse 事件中检查 Bash 命令，发现 `rm -rf` 时 `exit 2` 阻止执行，stderr 内容会反馈给 Agent（详见官方 Hooks 文档的 block-rm 示例）。

### 34.6 exit code 语义

- `0`：成功，stdout 按规则解析；
- `2`：阻塞，stderr 作为反馈传给 Agent；
- 其他：非阻塞错误，记录日志，主流程继续。

### 34.7 本章小结

- [ ] 知道三处配置文件的优先级与共享方式
- [ ] 会写 PostToolUse 自动 lint 的 Hook
- [ ] 理解 exit 2 的阻塞语义

# 第六部分 DevOps 与自动化

## 第35章 CLI 无头模式：把 AI 嵌入 CI/CD

### 35.1 Headless（无头）模式

`-p / --print` 让 Qoder CLI 非交互运行：接收提示词、执行任务、输出结果、退出。适合 Shell 脚本、自动化与 CI/CD。

```bash
qodercli -p "解释这个代码仓库的架构"
echo "总结昨天的代码变更" | qodercli -p
```

### 35.2 输出格式

| 格式 | 说明 | 适用 |
| --- | --- | --- |
| text | 纯文本（默认） | 直接阅读 |
| json | 单个 JSON 对象（结果+元数据） | 脚本解析 |
| stream-json | 逐条 JSON 消息流 | 实时消费 |

```bash
qodercli -p "重构 utils 模块" --output-format json
```

### 35.3 权限预配置

无头模式没有人确认，需要预先决定哪些操作自动执行：

```bash
# 文件编辑自动通过，shell 仍拒绝
qodercli -p "重构 utils 模块" --permission-mode accept_edits

# 只放行特定工具
qodercli -p "检查状态" --allowed-tools 'Read,Bash(git status)'

# 全部放行（仅限完全可信环境）
qodercli -p "执行数据库迁移" --yolo
```

### 35.4 CI 示例（GitHub Actions）

```yaml
name: AI Code Review
on:
  pull_request:
    types: [opened]
jobs:
  review:
    runs-on: ubuntu-latest
    env:
      QODER_PERSONAL_ACCESS_TOKEN: ${{ secrets.QODER_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - run: |
          qodercli -p "审查本次改动并列出潜在问题" \
            --output-format json \
            --permission-mode accept_edits \
            --max-turns 20
```

- 认证通过环境变量注入，密钥不落代码；
- 用 `--max-turns` 控制成本，用 json 输出供后续步骤解析。

### 35.5 常用参数

| 参数 | 作用 |
| --- | --- |
| -p / --print | 打印响应并退出 |
| -o / --output-format | text / json / stream-json |
| --max-turns | 限制最大轮数 |
| --permission-mode | 权限模式 |
| --allowed-tools / --disallowed-tools | 工具白/黑名单 |
| -m / --model | 指定模型 |
| -w / --cwd | 启动前切换目录 |

### 35.6 本章小结

- [ ] 会用 -p 运行无头任务
- [ ] 会选 json 输出并用脚本解析
- [ ] 能写一个 GitHub Actions 版 AI 审查流水线
## 第36章 定时任务与 /loop：让 Qoder 自动干活

### 36.1 定时任务

Qoder CLI 支持两种定时执行方式：

- **一次性定时**：在指定时间运行一次；
- **日历计划**：按日历计划重复运行（每天、每周…）。

适用场景：每日晨会报告、夜间回归测试、定期依赖更新检查。

### 36.2 /loop 重复执行

`/loop` 按固定时间间隔重复运行同一任务：

```
/loop 30m 检查 git 状态，若有未提交改动则提醒我
```

- 适合轮询类监控任务；
- 可以结合 Bash 与通知 Hook 推送结果。

### 36.3 定时任务 + 无头模式

定时任务的底层可以复用无头模式能力：

```bash
qodercli -p "生成今日代码变更摘要" --output-format text
```

再用系统 cron（Linux/macOS）或任务计划程序（Windows）调度即可：

```bash
0 9 * * * cd /repo && qodercli -p "生成昨日变更摘要" >> /tmp/daily.txt
```

### 36.4 实践：每日 PR 汇总

1. 写一条提示词：「汇总昨天合并到 main 的所有 PR 及其影响模块」；
2. 用定时任务每天 9 点执行；
3. 用 Notification Hook 推送桌面通知，或输出到文件供日报使用。

### 36.5 成本控制

- 定时任务消耗 credits，设置合理的频率与 `--max-turns`；
- 只对真正有产出的任务启用定时，避免浪费。

### 36.6 本章小结

- [ ] 会用 /loop 做轮询型任务
- [ ] 会把无头命令挂进 cron 实现定时执行
- [ ] 会评估定时任务的成本

## 第37章 一键部署与持续交付

### 37.1 /vercel-deploy 流程回顾

在 Quest 中执行 `/vercel-deploy`：

1. Qoder 检查项目并准备构建（安装依赖、生成产物）；
2. 引导完成 Vercel OAuth 授权（仅首次）；
3. 自动构建并部署到生产环境；
4. 返回线上链接与部署摘要。

### 37.2 部署到自己的服务器

让 Qoder 写部署脚本并执行：

```
帮我写一个部署脚本：构建前端产物、打包上传到服务器
192.168.1.10:/var/www/app，重启服务并做健康检查
```

- 涉及服务器操作时权限确认会触发，注意检查脚本内容；
- 敏感凭据用环境变量注入，不要写进代码。

### 37.3 Docker 化部署

```
为项目编写 Dockerfile 与 docker-compose.yml，
包含构建、运行、健康检查，并在本地跑通
```

- Qoder 会自动按项目技术栈生成合适的镜像配置；
- 审查生成的配置，确认基础镜像与暴露端口合理。

### 37.4 部署后的验证闭环

- 部署后让 Qoder 请求线上 URL 做冒烟测试；
- 失败时回滚：用 Git 回退到上一个稳定提交重新部署；
- 把「部署 → 验证 → 回滚」流程沉淀成 Skill 或规则。

### 37.5 本章小结

- [ ] 会用 /vercel-deploy 完成托管平台部署
- [ ] 会让 Qoder 生成服务器/Docker 部署方案
- [ ] 建立部署后验证与回滚的意识

## 第38章 终端、沙箱与权限控制

### 38.1 Quest 内置终端

- Quest 视窗内提供终端与沙箱环境，Agent 在其中运行命令；
- 你可以看到命令输出，随时干预。

### 38.2 沙箱机制

- 专家团（Experts）模式中，终端命令自动运行，**潜在危险命令自动在沙箱中执行**；
- 沙箱隔离降低误操作风险，但不等同于完全安全，仍需审查。

### 38.3 权限模型

| 模式 | 行为 |
| --- | --- |
| 默认（ask） | 写文件、跑命令前请求确认 |
| accept_edits | 自动批准工作区内安全文件编辑 |
| bypass_permissions | 跳过全部确认（`--yolo`，仅可信环境） |

### 38.4 用参数精细控制

```bash
# 只允许读与搜索
qodercli -p "梳理架构" --allowed-tools 'Read,Grep,Glob'

# 允许 git 状态类命令
qodercli -p "查看状态" --allowed-tools 'Read,Bash(git status)'
```

- 权限白名单比信任模型声明可靠；
- MCP 工具同样受白名单约束（见第30章）。

### 38.5 安全最佳实践

1. 陌生仓库用只读权限模式运行；
2. 部署、删除、写库类命令人工复核；
3. 密钥只走环境变量与 secrets；
4. 团队共享的 settings.json 中 Hooks 要经过审查。

### 38.6 本章小结

- [ ] 知道三种权限模式的区别
- [ ] 会用 --allowed-tools 收紧工具范围
- [ ] 建立「权限白名单优先于信任声明」的意识

## 第39章 GitHub Actions 与 Qoder Action

### 39.1 组合拳

- **Qoder CLI 无头模式**：在 Actions 里跑审查/生成任务（见第35章）；
- **Qoder Action**：官方 GitHub Actions 组件，在 PR/Issue 中直接做 AI 协作；
- 两者结合可实现「提交即审查、合并前门禁」。

### 39.2 Qoder Action 典型工作流

```yaml
name: AI Collaboration
on:
  pull_request:
    types: [opened, synchronize, reopened]
  issue_comment:
    types: [created]
jobs:
  qoder:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - name: Qoder AI
        uses: qoder/qoder-action@v1
        with:
          token: ${{ secrets.QODER_TOKEN }}
```

- 在 PR 评论里 @ 机器人即可触发对话式协作；
- 具体事件、输入参数以官方 Action 仓库文档为准。

### 39.3 组织级安装

- 在组织 Settings → Actions 中安装并授权仓库；
- 用组织的 secrets 统一管理令牌，成员无需各自配置。

### 39.4 自托管 Runner 注意

- 自托管机器上不要存明文令牌，使用 Actions Secrets；
- 限制 Runner 对生产环境的访问范围；
- 对不可信仓库的 PR 慎用写权限。

### 39.5 本章小结

- [ ] 会写一个 Qoder Action 的 workflow
- [ ] 知道组织级安装与 secrets 管理
- [ ] 了解自托管 Runner 的安全注意

# 第七部分 技术栈限定与项目配置

## 第40章 技术栈限定：Rules 规则系统

### 40.1 为什么需要限定

大模型靠通用知识工作，缺少项目专属上下文。**Rules 通过把预定义上下文注入提示**，让 AI 输出始终符合你项目的框架、代码风格与标准。

### 40.2 四种规则类型

| 类型 | 说明 | 使用场景 |
| --- | --- | --- |
| 手动引入 | 用 `@rule` 手动应用 | 按需工作流、自定义提示词 |
| 模型决策 | 模型根据描述判断何时应用 | 场景化任务（生成单测、注释） |
| 始终生效 | 适用于所有会话请求 | 强制项目级标准 |
| 指定文件生效 | 匹配通配符的文件才生效 | 语言/目录特定规则 |

### 40.3 存储与共享

- 规则存放于 `.qoder/rules` 目录，**仅对当前项目生效**；
- 规则文件随 Git 提交，与团队成员共享；
- 仅本地使用的规则：把 `.qoder/rules` 加进 `.gitignore`。

### 40.4 配置步骤

1. 打开 Qoder 设置（`Ctrl + Shift + ,`）→ **规则**；
2. 点击**添加**，输入唯一规则名称并确认；
3. 选择规则类型并填写内容：
   - 模型决策：输入场景描述，如「生成一个单元测试」；
   - 指定文件生效：输入通配符，如 `*.md`、`src/*.java`；
   - 始终生效：直接编写规则正文；
4. 关闭窗口保存。

### 40.5 示例：限定技术栈

```markdown
# 技术栈约束
- 本项目使用 Bun + TypeScript + Hono，禁止引入 Express
- 使用项目统一的错误码枚举，不新建字符串错误
- 数据访问必须走 src/db 封装层，禁止直接拼 SQL
- 提交前运行 bun test 与 bun run lint
```

### 40.6 限制与最佳实践

- 所有活跃规则文件合计**最多 100,000 字符**（超出截断）；
- 只支持自然语言，不支持图片或链接；
- 保持简洁、结构清晰、包含示例、持续迭代；
- AGENTS.md 与规则冲突时，**规则内容优先**。

### 40.7 本章小结

- [ ] 理解四种规则类型与适用场景
- [ ] 会创建一条「始终生效」的技术栈规则
- [ ] 知道规则通过 Git 共享、冲突时优先于 AGENTS.md
## 第41章 AGENTS.md 与记忆系统

### 41.1 三层记忆概览

Qoder 每次会话都会重新构造上下文，跨会话保留的知识分为：

- **静态记忆**：由你或团队维护的持久指令（AGENTS.md 与 rules）；
- **自动记忆**：Qoder 在本机自动保存的 Markdown 记忆（需启用）；
- **结构化知识库**：按模块沉淀的文档（Repo Wiki 等）。

> 记忆提供上下文，**不是安全边界**；强制策略要用权限与 Hooks。

### 41.2 静态记忆文件位置

| 位置 | 用途 | 是否提交 |
| --- | --- | --- |
| `~/.qoder/AGENTS.md` | 个人跨项目偏好 | 否 |
| `<项目>/AGENTS.md` | 团队项目规则、架构说明 | 是 |
| `<项目>/AGENTS.local.md` | 本机私有说明 | 否 |
| `<项目>/.qoder/rules/**/*.md` | 按主题/文件范围拆分的规则 | 是 |

### 41.3 AGENTS.md 写什么

```markdown
# Development

- 使用 bun test 与 bun run lint 验证改动
- API handler 位于 src/api/handlers/
- 不要修改 src/generated/ 下的生成文件
- 提交信息使用 Conventional Commits

See @README.md for architecture notes.
Use @docs/testing.md for test data setup.
```

- `@path` 可导入其他文件（相对 AGENTS.md 所在目录）；
- 只写「下次会话还要知道的事实」，不写临时状态；
- 指令越具体越稳定。

### 41.4 用 /init 起步

在项目根目录输入 `/init`，Qoder 分析项目并生成初始 AGENTS.md，这是接入记忆系统最快的起点。

### 41.5 自动记忆

```bash
QODER_MEMORY=1 qodercli        # 启用项目级自动记忆
QODER_MEMORY=1 QODER_MEMORY_USER=1 qodercli  # 同时启用用户级
```

- 自动保存四类内容：user（偏好）、feedback（纠正）、project（背景）、reference（外部资料位置）；
- 存储在 `~/.qoder/projects/<项目>/memory/`；
- `/memory` 查看概览，`/memory manage` 管理主题文件。

### 41.6 让 Qoder 记住或忘记

```
记住：集成测试需要先启动本地 Redis。
忘记之前关于旧部署脚本的记忆。
把这条测试约定加到项目 AGENTS.md。
```

### 41.7 排查：Qoder 没遵守 AGENTS.md

- 运行 `/memory` 确认文件出现在列表；
- 确认目录在可信工作区内；
- 检查用户级/项目级/本地级是否有冲突指令。

### 41.8 本章小结

- [ ] 会用 /init 生成 AGENTS.md
- [ ] 知道四个记忆文件位置与提交策略
- [ ] 会启用自动记忆并管理主题文件

## 第42章 模型选择与自定义模型

### 42.1 模型选择器

Chat 面板底部（或 CLI 中）可以选择模型。不同模型在能力、速度、成本上差异明显：

- **旗舰/Ultimate 类**：复杂推理、长任务，credits 消耗高；
- **平衡/Performance 类**：日常编码性价比之选；
- **快速模型**：补全、简单问答，成本低。

### 42.2 可调参数

部分模型支持参数配置：

- 上下文窗口大小；
- 思考开关（是否启用深度思考）；
- 思考预算（限制推理长度以控制成本）。

### 42.3 按任务选模型

| 任务 | 建议 |
| --- | --- |
| 简单问答 / 补全 | 快速模型 |
| 日常编码 / 重构 | 平衡模型 |
| 复杂架构 / 长任务 | 旗舰模型 + 深度思考 |
| 固定成本预算 | 统一用平衡模型 + 限制 turns |

### 42.4 自定义模型

- 支持通过 API 密钥接入自己的模型（OpenAI 兼容接口等）；
- 在设置 → 模型 → 添加自定义模型中配置；
- 适合企业内部模型或成本控制场景。

### 42.5 成本意识

- 消耗 = 输入 token + 输出 token，与模型单价相关；
- CLI 中 `/usage` 查看 Credits 使用情况；
- 长任务用 `--max-turns`、定时任务设合理频率，防止失控消耗。

### 42.6 本章小结

- [ ] 知道模型选择器与三层模型定位
- [ ] 会按任务类型选择模型
- [ ] 会添加自定义模型并查看 /usage 成本

## 第43章 上下文控制：让 AI 看到该看的

### 43.1 上下文由什么组成

- 对话历史；
- 项目说明（AGENTS.md）与规则（rules）；
- 长期记忆与知识库检索结果；
- 你通过 @ 引用的文件与选中代码。

### 43.2 主动注入上下文

- 输入 `@` 选择文件，把关键文件拉进对话；
- 先选中代码再提问，聚焦范围；
- 提问时补充背景：「这是一个 Bun + Hono 的 API 项目」。

### 43.3 上下文过多怎么办

- 长会话后输入 `/compact` 压缩上下文（保留摘要）；
- 用 `/clear` 开始新会话，让 Qoder 重新加载记忆；
- 把不相关文件移出 @ 引用；
- 大型项目用知识库按需检索，而不是全量注入。

### 43.4 上下文过少怎么办

- 问题里补足必要信息：技术栈、文件路径、预期行为、失败现象；
- 用 Ask 先问「这个模块在哪里、怎么组织的」，再深入；
- 把关键约定写进 AGENTS.md，让每次会话自动带上。

### 43.5 子目录记忆

- 从项目根目录启动不会预加载子目录的 AGENTS.md；
- Qoder 访问子目录文件后才会按需补充该层记忆；
- 大仓库可分层放置 AGENTS.md 控制上下文体积。

### 43.6 本章小结

- [ ] 会用 @ 引用与选中代码主动给上下文
- [ ] 会用 /compact 与 /clear 控制上下文体积
- [ ] 理解子目录记忆的按需加载机制

## 第44章 团队协作：共享规则与知识库

### 44.1 把约定交给 Git

- `AGENTS.md`、`.qoder/rules/` 随代码提交，全员自动生效；
- 新成员 clone 仓库即获得完整项目规范，上手更快；
- `.gitignore` 排除个人级与本地级文件（如 AGENTS.local.md）。

### 44.2 分层知识体系

| 层次 | 内容 | 归属 |
| --- | --- | --- |
| 项目说明 | 全局约定、命令、结构 | 仓库提交 |
| 规则文件 | 按主题/文件范围拆分 | 仓库提交 |
| 长期记忆 | 个人偏好、项目背景 | 本机 |
| 知识模块 | 子系统架构说明 | 仓库提交 |

### 44.3 Repo Wiki 与知识卡片

- Repo Wiki 自动沉淀项目知识，生成文档与架构图谱；
- Knowledge Cards（知识卡片）把关键结论做成可检索条目；
- 团队可以在「知识引擎」中统一查看与维护。

### 44.4 企业级 QMind

- 企业版提供 QMind 知识库，支持组织级知识沉淀；
- 适合跨项目复用团队经验与安全规范；
- 管理员可控制谁能读取与修改知识。

### 44.5 团队工作流建议

1. 技术负责人维护 AGENTS.md 与 rules；
2. 成员提交 PR 时，Code Review Agent 按规则自动审查；
3. 每次审查后的新约定回写规则文件；
4. 定期用 /remember 整理记忆，晋升有价值内容到项目说明。

### 44.6 本章小结

- [ ] 知道哪些文件应提交、哪些应 gitignore
- [ ] 理解三层知识体系的分工
- [ ] 会设计团队规则维护与审查闭环

# 第八部分 提示词工程

## 第45章 提示词工程基础：为什么重要

### 45.1 提示词决定输出质量

同样的任务，描述方式不同，AI 产出可能天差地别。提示词工程 = 与 AI 高效沟通的能力，包括具体性、上下文组织、外部工具调用与迭代。

### 45.2 好提示词三原则

- **具体**：说清对象、操作、约束，不说模糊的大词；
- **简洁**：不啰嗦，信息密度高；
- **明确**：一次只聚焦一个目标，验收标准可检验。

### 45.3 对比示例

❌ 模糊：

```
帮我优化这个代码
```

✅ 明确：

```
优化 src/orders.ts 的 getOrders 函数：
1. 把双重循环改成一次遍历，保持返回结构不变
2. 补上空数组边界处理
3. 用项目现有的 vitest 补测试并运行验证
```

### 45.4 常见结构模板

```
【背景】项目情况、技术栈、相关文件
【目标】要达成的最终效果
【约束】命名、风格、不允许做的事
【验收】如何判断成功（跑哪条命令）
【输出】期望的交付形式（代码/报告/表格）
```

### 45.5 针对 Qoder 场景的适配

- Ask：问「为什么/怎么做」，给出上下文文件；
- Agent：给「目标 + 约束 + 验收」，让它自主执行；
- Quest：给「完整需求 + 质量标准」，先审 Spec 再执行；
- NEXT/行间：短指令 + 选中代码，就地修改。

### 45.6 本章小结

- [ ] 掌握具体/简洁/明确三原则
- [ ] 会用背景-目标-约束-验收-输出结构
- [ ] 能区分 Ask / Agent / Quest 的不同写法
## 第46章 结构化任务描述：让 Agent 一次做对

### 46.1 任务描述 = 目标 + 约束 + 验收

Agent 模式的提示词应当像一份迷你需求文档：

```
目标：新增用户注册接口 POST /api/register
约束：
- 使用项目现有的 validator 做参数校验
- 密码用 bcrypt 哈希存储，禁止明文
- 遵循现有 handler 的错误返回结构
验收：
- bun test 通过
- 用 curl 调通成功与参数错误两种场景
```

### 46.2 分层描述模板

| 层 | 内容 | 示例 |
| --- | --- | --- |
| 背景 | 为什么做、在哪做 | 登录模块缺注册功能，接口在 src/api/ |
| 目标 | 做什么、到什么程度 | 实现注册接口 + 单测 |
| 步骤 | 可选：关键顺序 | 先写 schema，再写 handler，最后测试 |
| 验收 | 可验证的标准 | 测试通过、curl 可调通 |
| 输出 | 交付形式 | 代码 + 变更说明 |

### 46.3 错误示范 vs 正确示范

❌ 「把登录做一下」——没有范围、没有标准。

✅

```
实现登录接口：
1. 支持邮箱+密码登录，密码用 argon2 校验
2. 成功后签发 30 分钟 JWT，存 HttpOnly cookie
3. 失败统一返回 { code: 40101, message }
4. 为 handler 补 3 个单测并运行验证
```

### 46.4 补充与迭代

- 首次输出后检查，把差异告诉它：「错误码字段名用 errCode，不是 code」；
- 一次只改一类问题，避免多轮信息过载；
- 满意后说「记住这条约定」，沉淀进记忆/规则。

### 46.5 长任务的分解式描述

大需求先给整体目标，再要求分阶段交付：

```
分三步完成用户模块：
第一步：设计数据模型与接口清单，给我确认
第二步：实现核心 CRUD
第三步：补测试与文档
每步完成都停下来等我确认
```

### 46.6 本章小结

- [ ] 会用「目标+约束+验收」三段式写任务
- [ ] 会把大需求拆成分步确认的 Quest
- [ ] 会通过一次一类反馈迭代输出

## 第47章 提示词增强与迭代调优

### 47.1 提示词增强是什么

Qoder 提供**提示词增强（Prompt Enhancement）**功能：把你输入的自然语言自动改写/补全成更完整、更利于模型执行的提示词，降低手动写提示词的门槛。

### 47.2 什么时候用

- 不确定自己的描述够不够完整 → 让增强帮忙；
- 简单的两句话需求 → 增强后执行效果更好；
- 团队新手 → 借助增强学习「好提示词长什么样」。

### 47.3 什么时候不用

- 已经写了结构化任务描述（第46章）时，增强可能画蛇添足；
- 对输出格式有严格要求时，保持原样更可控。

### 47.4 迭代调优流程

1. **写**：按模板写出初版提示词；
2. **跑**：执行并观察输出；
3. **差**：对比预期，找出偏差（范围、风格、缺验收）；
4. **改**：把偏差转成一句话反馈，重新提问；
5. **沉淀**：把反复有效的写法固化进 Skill / 规则 / 记忆。

### 47.5 常见失败模式与对策

| 失败现象 | 对策 |
| --- | --- |
| 改错文件/范围 | 明确文件路径与「只改 X，不动 Y」 |
| 风格不符 | 给一个「参考现有代码」的例子 |
| 漏验收 | 明确「完成后运行哪条命令」 |
| 过度发挥 | 加约束「不要引入新依赖」「不要重构无关代码」 |
| 反复犯错 | 用 /remember 或规则固化纠正 |

### 47.6 本章小结

- [ ] 会用提示词增强降低描述成本
- [ ] 掌握「写→跑→差→改→沉淀」迭代五步
- [ ] 熟悉五类常见失败模式的对策

## 第48章 长任务、上下文压缩与成本控制

### 48.1 长任务的两个挑战

- **上下文衰减**：历史过长，模型容易丢失早期细节；
- **成本膨胀**：token 消耗随轮数线性增长。

### 48.2 应对上下文衰减

- 关键信息写进 AGENTS.md/rules，不依赖对话历史；
- 长任务用 Quest 的 Spec 确认，早期就把需求定稿；
- 上下文接近上限时主动 `/compact` 压缩；
- 大改动分阶段提交，每阶段独立审查。

### 48.3 应对成本膨胀

- 选对模型：长任务选平衡模型 + 合理思考预算；
- 限制轮数：无头模式用 `--max-turns`；
- 及时 `/usage` 检查消耗；
- 定时任务与 /loop 设低频、短任务；
- 简单问题别开深度思考。

### 48.4 让 Qoder 自己控制成本

```
实现这个功能，但保持高效：
- 尽量复用现有工具函数，不新造轮子
- 一次完成，不要中途反复确认
- 完成后只报告：改动文件列表 + 测试结果
```

### 48.5 追踪与复盘

- 每周查看 credits 消耗，按任务类型归因；
- 把高频高成本任务固化成 Skill，减少重复探索；
- 长会话结束前让 Qoder 输出「决策摘要」，沉淀到记忆。

### 48.6 本章小结

- [ ] 会用 Spec 确认与 /compact 对抗上下文衰减
- [ ] 会从模型、轮数、频率三处控制成本
- [ ] 会复盘 credits 消耗并固化高效模式

# 第九部分 实战与进阶

## 第49章 综合实战：从需求到上线全流程

### 49.1 场景

用 Qoder 从零实现一个「团队内部记事本」Web 应用并上线，走完完整工作流。

### 49.2 步骤一：需求定稿（Ask + Quest）

```
用 Quest 帮我设计「团队记事本」：
技术栈 Bun + TypeScript + Hono + SQLite
功能：注册登录、笔记 CRUD、按标签筛选
要求：先输出数据模型与接口清单，我确认后再实现
```

- 审阅 Spec 与计划，确认后放行。

### 49.3 步骤二：实现（Experts / Agent）

```
在 Quest 用专家团模式实现：前后端分离，
认证用 JWT，笔记按用户隔离，禁止 SQL 注入，
完成后运行全部测试
```

- 观察任务列表与专家状态，必要时插入纠正。

### 49.4 步骤三：审查（Review + Code Review Agent）

1. 打开 Review 面板，逐文件看 diff；
2. 对可疑行加批注：「这里要校验笔记归属」；
3. 让 Code Review Agent 跑一遍：「按 code-review 规则审查本次改动」；
4. 修复 P0/P1 后重新测试。

### 49.5 步骤四：提交与 PR

```
按 Conventional Commits 提交本次改动，创建分支 feature/notes-app，
生成 PR 描述（含功能、测试、截图说明）
```

### 49.6 步骤五：部署（/vercel-deploy）

- 执行 `/vercel-deploy` 完成 OAuth 与部署；
- 打开线上链接做冒烟测试（注册→登录→建笔记→筛选）；
- 失败则回滚：修复后重新部署。

### 49.7 步骤六：沉淀

- 把「部署流程」「接口规范」写进 AGENTS.md；
- 把「团队代码审查规则」存为 rules / Skill；
- 一个项目跑通，之后每个新项目都能复用这套流程。

### 49.8 本章小结

- [ ] 走完 需求→实现→审查→提交→部署 全流程
- [ ] 体验 Quest + Experts + Review + CLI 的组合用法
- [ ] 完成一次项目级知识沉淀

## 第50章 常见问题、安全边界与资源汇总

### 50.1 常见问题速查

| 问题 | 处理 |
| --- | --- |
| 登录失败 | 检查网络（国际版可能需要代理）、清理浏览器缓存 |
| credits 不够 | /usage 查看消耗，调低模型档位，或升级订阅 |
| AI 改错文件 | 明确路径与范围，用 Review 面板回退（Discard） |
| 上下文乱 | /compact 压缩或 /clear 新会话 |
| 规则没生效 | /memory 确认加载；检查是否在可信工作区 |
| 定时任务浪费 | 降低频率、加 --max-turns、只保留有效任务 |

### 50.2 安全边界（务必牢记）

- Skills 与记忆**只是上下文，不是安全边界**；
- 强制策略必须用**权限配置（--allowed-tools、permission-mode）与 Hooks**；
- MCP 服务器自声明能力不可信，一律白名单控制；
- 密钥只走环境变量与 secrets，不进代码与提交。

### 50.3 数据与合规

- 国际版/国内版数据政策不同，按团队合规要求选择版本；
- 涉密代码慎用云端模型，评估企业版私有化方案；
- 公司敏感仓库先看许可与数据协议再使用 AI。

### 50.4 官方资源汇总

| 资源 | 地址 |
| --- | --- |
| 国际版官网 | qoder.com |
| 国际版文档 | docs.qoder.com |
| 国内版官网 | qoder.cn |
| 国内版文档 | docs.qoder.cn |
| 社区论坛 | forum.qoder.com |
| 技能市场 | skills.sh |

### 50.5 持续学习

- 关注官方博客的产品更新与提示词增强等新功能；
- 订阅 Release Notes，掌握 Quest / Experts / Skills 的演进；
- 把本教程的每章小结当作 checklist，逐项打勾即完成入门；
- 深入方向：MCP 工具开发、企业 QMind、SDK 集成。

### 50.6 毕业清单

- [ ] 能独立用 Qoder 完成一个小项目并上线
- [ ] 掌握 Git、Skills、代码审查、DevOps 四块核心能力
- [ ] 会用规则与提示词工程约束 AI 输出
- [ ] 建立了安全与成本意识，能持续自我提升

> 恭喜完成 50 章学习！Qoder 是一个快速演进的产品，保持关注官方文档，你的技能会随着版本一起成长。
